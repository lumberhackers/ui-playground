{
  "name": "mfe-beam",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "serve:dist": "npx http-server --cors --ssl -C certs/localhost.pem -K certs/localhost-key.pem dist"
  },
  "dependencies": {
    "vue": "^3.2.37",
    "vue-router": "4",
    "webcam-easy": "^1.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.0",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vue-tsc": "^0.40.4"
  }
}
